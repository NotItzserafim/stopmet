import { HNode } from "./HNode";
import { Store } from "./Store";
import { DeferCallback } from "./ticker";
export declare const renderCallbacks: DeferCallback<[]>[];
export interface RenderOptions {
    clear?: boolean;
    context?: Store;
    parent?: Node;
    owner?: HNode<any>;
    sync?: boolean;
}
export declare const render: (src: any, options?: RenderOptions) => void;
